ninja  # For faster builds.
psutil
ray >= 2.9.0
pandas == 2.0.3  # Required for Ray data.
pyarrow == 12.0.1  # Required for Ray data.
sentencepiece  # Required for LLaMA tokenizer.
numpy~=1.23.0
transformers >= 4.38.0  # Maintain model availability, recommended transformers>=4.38.0 for Gemma, qwen1.5 and so on...
fastapi
uvicorn[standard]
pydantic >= 2.0  # Required for OpenAI server.
prometheus_client >= 0.18.0
jsonlines
openai
aiohttp
tiktoken
outlines >= 0.0.34
# pynvml == 11.5.0
# triton >= 2.1.0
# outlines >= 0.0.27
# cupy-cuda12x == 12.1.0  # Required for CUDA graphs. CUDA 11.8 users should install cupy-cuda11x instead.